{% if calendar_days %}
  {% for day in calendar_days %}
    {% if day.events %}
{b}{{ day.date.strftime("%a %-d %B") }}{/b}
      {% for ev in day.events %}
{b}{{ ev.summary }}{/b}
        {% if ev.location %}
@{{ ev.location }}
        {% endif %}
        {% if ev.description %}
{i}{{ ev.description }}{/i}
        {% endif %}
        {% if not loop.last %}
{{ line_single }}{% endif %}
      {% endfor %}
    {% endif %}
  {% endfor %}
{% else %}
No calendar events
{% endif %}